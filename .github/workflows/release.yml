name: Build Syndo Release Artifacts
on:
  push:
    branches:
      - release-*
    tags:
      - release-*
jobs:
  metadata:
    runs-on: ubuntu-latest
    steps:
      - name: Capture pom version
        id: pom
        run: |
          export POM_VER=$(mvn org.apache.maven.plugins:maven-help-plugin:3.2.0:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "POM_VER=${POM_VER}" >> $GITHUB_ENV
          echo "::set-output name=pom-ver::${POM_VER}"

      - name: Capture branch info
        id: decide-prerelease
        if: contains(github.ref, 'refs/heads/')
        run: |
          echo "PRERELEASE=true" >> $GITHUB_ENV
          echo "::set-output name=decide::true"

      - name: Capture branch info
        id: decide-release
        if: "!contains(github.ref, 'refs/heads/')"
        run: |
          echo "PRERELEASE=false" >> $GITHUB_ENV
          echo "::set-output name=decide::false"

    outputs:
      pom-ver: ${{ steps.pom.outputs.pom-ver }}
      prerelease: ${{ steps.decide-prerelease.outputs.decide }}${{ steps.decide-release.outputs.decide }}

  verify:
    needs: metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          java-version: '8'
          distribution: 'adopt'
      - name: Build with Maven
        run: mvn --batch-mode --update-snapshots verify

  prepare:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - name: Delete release/tag if it exists
        uses: dev-drprasad/delete-tag-and-release@v0.2.0
        with:
          delete_release: true
          tag_name: ${{ github.ref }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build_jar:
    # dependency on verification
    needs: [metadata, prepare]

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          java-version: '8'
          distribution: 'adopt'

      - name: Build with Maven
        run: mvn --batch-mode --update-snapshots -DskipTests package

      - name: Upload java binary to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: target/syndo-${{ env.POM_VER }}.jar
          asset_name: syndo-${{ env.POM_VER }}.jar
          tag: ${{ github.ref }}
          overwrite: true
          prerelease: ${{ env.PRERELEASE }}

      - name: Upload java executable binary to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: target/syndo-${{ needs.metadata.outputs.pom-ver }}-shaded.jar
          asset_name: syndo-${{ needs.metadata.outputs.pom-ver }}-executable.jar
          tag: ${{ github.ref }}
          overwrite: true
          prerelease: ${{ needs.metadata.outputs.prerelease }}

  build_native:
    # dependency on verification
    needs: [metadata, prepare]

    # run this job on the current template instantiation
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            gu-binary: gu
            native-asset-suffix: linux-amd64
          - os: windows-2016
            gu-binary: gu.cmd
            native-asset-suffix: windows-amd64
    steps:
      # install GraalVM for native build
      - uses: DeLaGuardo/setup-graalvm@4.0
        with:
          graalvm: '21.0.0.2'
          java: 'java11'
          arch: 'amd64'

      - name: Add msbuild to PATH
        if: contains(matrix.os, 'windows')
        uses: microsoft/setup-msbuild@v1.0.2

      - name: Add toolkit to path
        if: contains(matrix.os, 'windows')
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install native-image component
        run: |
          ${{ matrix.gu-binary }} install native-image

      - uses: actions/checkout@v2

      - name: Build with Maven
        run: mvn --batch-mode --update-snapshots -DskipTests -Pnative package

      - name: Upload native binary to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: target/syndo
          asset_name: syndo-${{ needs.metadata.outputs.pom-ver }}-${{ matrix.native-asset-suffix }}
          tag: ${{ github.ref }}
          overwrite: true
          prerelease: ${{ needs.metadata.outputs.prerelease }}